telethon>=1.0                 # The core library
yt-dlp                        # Audio fetching (stays the same)
requests                      # Proxy requests (stays the same)
uvicorn                       # Proxy server (stays the same)
fastapi                       # Proxy server framework (stays the same)
pytgcalls[telethon]
